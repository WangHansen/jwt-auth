export class CronJob {
  constructor(
    cronTime,
    onTick,
    onComplete,
    start,
    timezone,
    context,
    runOnInit,
    unrefTimeout
  ) {
    // mock cronjob
  }

  start() {
    // strat
  }

  stop() {
    // stop
  }
}
